###############################################################################
## Search Macros for MCP and LLM Indexes
###############################################################################

# MCP Index macro - for JSON-RPC data
[mcp_index]
definition = index=mcp
iseval = 0

# LLM Index macro - for Ollama/LLM data
[llm_index]
definition = index=llm
iseval = 0

# Combined AI/LLM infrastructure macro
[ai_index]
definition = (index=mcp OR index=llm)
iseval = 0
